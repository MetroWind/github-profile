query($count: Int!) {
  viewer {
    repositories(first: $count, isFork: false) {
      edges {
        node {
          name
          languages(first: 10) {
            edges {
              node {
                name
              }
              size
            }
          }
        }
      }
    }
  }
}
